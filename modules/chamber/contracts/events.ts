/**
 * Chamber module — event contract (stub).
 *
 * No events are published by Chamber in v0.2. This file exists so
 * MODULE.toml's contracts.events path resolves and so future event
 * definitions land here rather than in the module's private surface.
 */

export {}
